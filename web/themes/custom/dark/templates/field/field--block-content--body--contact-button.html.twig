{%
  set title_classes = [
    label_display == 'visually_hidden' ? 'visually-hidden',
  ]
%}

{% if label_hidden %}
    {% for item in items %}
      <a href="#contacts"><div{{ attributes }}>{{ item.content }}</div></a>
    {% endfor %}
{% endif %}